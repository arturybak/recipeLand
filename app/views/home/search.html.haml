.squircle.jumbotron
  %h2
    %strong= @results.size
    =t('.results')
    %strong= @parameter
%br
- @results.each do |recipe|
  .col-md-4
    .squircle
      = link_to recipe do
        = image_tag(recipe.image, size: "400x400") if recipe.image.attached?
      .title
        %h2= link_to recipe.title, recipe
